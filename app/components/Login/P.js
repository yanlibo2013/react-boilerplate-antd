import styled from 'styled-components';

const div = styled.div`
  position: absolute;

  color: #ffffff;
  font-size: 26px;
  font-weight: 400;
  margin: 200px 0 0 0;
  letter-spacing: 2.6px;
`;

export default div;
